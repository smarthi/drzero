# Core framework
restate-sdk>=0.4.0
hypercorn>=0.17.0
starlette>=0.35.0

# Web/HTTP
httpx>=0.27.0

# Numerical (for embeddings)
numpy>=1.24.0

# UI
streamlit>=1.31.0

# Optional: OpenSearch client
opensearch-py>=2.4.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# =============================================================================
# Training Dependencies (for real LLM fine-tuning)
# =============================================================================
# Install with: pip install torch transformers peft trl datasets accelerate

# PyTorch - use appropriate version for your CUDA version
# torch>=2.0.0

# Transformers and training utilities
# transformers>=4.36.0
# peft>=0.7.0        # LoRA/QLoRA adapters
# trl>=0.7.0         # DPO/PPO training
# datasets>=2.16.0   # HuggingFace datasets
# accelerate>=0.25.0 # Distributed training
# bitsandbytes>=0.41.0  # 8-bit quantization
